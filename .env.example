# Environment
# Local dev (unix socket, peer auth as current OS user):
DATABASE_URL=postgresql+psycopg2:///agent_platform
# Supabase (later):
# DATABASE_URL=postgresql+psycopg2://postgres.<ref>:<password>@aws-0-<region>.pooler.supabase.com:6543/postgres
JWT_SECRET=change-me-in-prod
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# LLM (OpenRouter — OpenAI-compatible)
OPENAI_API_KEY=sk-or-v1-xxxx
OPENAI_BASE_URL=https://openrouter.ai/api/v1
LLM_MODEL=deepseek/deepseek-v4-flash

# Default model used when a project has no explicit model
DEFAULT_MODEL=deepseek/deepseek-v4-flash

# Optional secondary provider — used automatically when the primary LLM call
# fails (rate limit / connection / 5xx) before any token is yielded. The
# fallback model id must exist on the fallback provider.
OPENAI_FALLBACK_API_KEY=sk-or-v1-xxxx
OPENAI_FALLBACK_BASE_URL=https://openrouter.ai/api/v1
OPENAI_FALLBACK_MODEL=deepseek/deepseek-v4-flash

# --- Step 6: RAG / file upload (Part A) ---

# Heavy-deps relocation (local dev only — keep off the tight /home partition).
# Must also be exported to the shell when running the server/tests, e.g.:
#   export HF_HOME=/opt/hf-cache PIP_CACHE_DIR=/opt/pip-cache
HF_HOME=/opt/hf-cache
PIP_CACHE_DIR=/opt/pip-cache

# Embedding model (nomic-ai/nomic-embed-text-v1.5, dim 768) — task-prefixed
EMBEDDING_MODEL=nomic-ai/nomic-embed-text-v1.5
EMBEDDING_DIM=768

# Supabase (Storage + Postgres)
SUPABASE_URL=https://<ref>.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJ...
SUPABASE_STORAGE_BUCKET=project-files

# Upload limits
MAX_UPLOAD_BYTES=10485760

# --- Optional web search (Tavily) ---
# Leave empty to disable web search: the web_search tool is then excluded
# from the tools offered to the model (never offered-then-failed).
TAVILY_API_KEY=
